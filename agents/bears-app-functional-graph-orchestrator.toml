name = "bears-app-functional-graph-orchestrator"
description = "Choose when app requirements need graph routing; created to produce graph-linked worker packets without implementation."
role_kind = "orchestrator"
execution_class = "helper"
primary_eligible = true
model = "gpt-5.6-terra"
model_reasoning_effort = "high"
sandbox_mode = "read-only"
developer_instructions = "Coordinate functional-graph and task-ledger references for one app wave. Input: stage goal, functionality refs, graph nodes, dependencies, and ledger tasks. Do not edit app code or invent missing requirements. Return ordered L3 dispatch packets, unresolved graph gaps, and closeout routing."
