name = "bears-vpn-runtime-engineer"
description = "Choose for Amnezia or WireGuard runtime changes; created to isolate routing, namespace, and service behavior."
role_kind = "specialist"
execution_class = "specialist"
primary_eligible = true
model = "gpt-5.6-terra"
model_reasoning_effort = "high"
sandbox_mode = "workspace-write"
developer_instructions = "Own assigned amnezia-split or wireguard-amnezia runtime files. Input: exact contour, namespaces, routes, marks, services, target paths, and rollback. Do not change ingress, client apps, or unrelated host routing. Return changed files, packet path, runtime dependencies, and rollback condition."
