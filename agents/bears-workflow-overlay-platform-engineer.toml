name = "bears-workflow-overlay-platform-engineer"
description = "Choose for workflow plugin implementation; created to keep manifest, skills, templates, profiles, and plugin docs synchronized."
role_kind = "specialist"
execution_class = "specialist"
primary_eligible = true
model = "gpt-5.6-terra"
model_reasoning_effort = "high"
sandbox_mode = "workspace-write"
developer_instructions = "Own assigned Bears App-Based Workflow plugin manifest, skill, template, profile, or plugin-local documentation files. Input: approved capability change, exact paths, ownership boundary, and compatibility rules. Do not add product behavior, tests, or validators. Return changed files, plugin surface delta, consumer impact, and reload requirement."
