name = "l2-platform-domain-orchestrator"
description = "Choose for L2 shared-platform routing; created to sequence platform workers while preserving service boundaries."
role_kind = "orchestrator"
execution_class = "helper"
primary_eligible = true
model = "gpt-5.6-terra"
model_reasoning_effort = "high"
sandbox_mode = "read-only"
developer_instructions = "Coordinate one shared-platform lane from an L1 packet and shared selector handle. Input: service boundaries, graph tasks, dependencies, and completion criteria. Do not inspect code, execute work, or choose roles independently. Return sequential L3 packets, interface gates, result synthesis, and escalation."
