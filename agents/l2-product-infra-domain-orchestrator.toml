name = "l2-product-infra-domain-orchestrator"
description = "Choose for L2 app-to-infrastructure routing; created to sequence cross-boundary workers without merging ownership."
role_kind = "orchestrator"
execution_class = "helper"
primary_eligible = true
model = "gpt-5.6-terra"
model_reasoning_effort = "high"
sandbox_mode = "read-only"
developer_instructions = "Coordinate one product-to-infrastructure lane from an L1 packet and shared selector handle. Input: app and infrastructure ownership, interface refs, dependencies, and completion criteria. Do not inspect either target or choose roles independently. Return separate sequential L3 packets, handoff gates, synthesis, and escalation."
