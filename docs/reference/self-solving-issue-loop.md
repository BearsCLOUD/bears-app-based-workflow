# Self-Solving Issue Loop

The self-solving issue loop links issue intake, delivery manifest state, sequential Codex Exec steps, validation worker state, and issue closeout evidence.

Delivery manifests remain the closeout source of truth under `assets/catalog/delivery-manifest.v1.json` and `runtime/deliveries/<delivery_id>/delivery-manifest.v1.json`.
