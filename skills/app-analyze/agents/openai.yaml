interface:
  display_name: "App Analyze"
  short_description: "Analyze repo waves and remediation"
  default_prompt: "Use $app-analyze to assess one repo's implementation, immutable reviews, remediation, and exact autoCI evidence against wave artifacts."
