interface:
  display_name: "App Analyze"
  short_description: "Check app graph and ledger drift."
  default_prompt: "Use $app-analyze to check App Target Gate, constitution, research, spec, app functional graph, app task ledger, Project status refs, notification refs, lanes, roles, proof requirements, dependencies, and app-dev handoff."
