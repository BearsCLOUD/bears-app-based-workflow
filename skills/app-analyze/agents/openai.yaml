interface:
  display_name: "App Analyze"
  short_description: "Check lineage and file reuse quality"
  default_prompt: "Use $app-analyze to compare constitution, research, plan, graph, ledger, implementation, and plugin-file reuse quality."
