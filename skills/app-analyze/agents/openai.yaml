interface:
  display_name: "App Analyze"
  short_description: "Validate and atomically audit a wave"
  default_prompt: "Use $app-analyze to compare workflow semantics, validate the exact SQLite-backed snapshot, and atomically attest audited."
