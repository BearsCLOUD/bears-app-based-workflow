interface:
  display_name: "App Constitution"
  short_description: "Register a project and start a wave"
  default_prompt: "Use $app-constitution to register the Git project, initialize one owned wave, and record its constitution phase through MCP."
