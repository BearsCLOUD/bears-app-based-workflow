interface:
  display_name: "App Constitution"
  short_description: "Create the app-local workflow baseline"
  default_prompt: "Use $app-constitution under the current stage owner to record the app boundary, constraints, decision source, and research unknowns."
