interface:
  display_name: "App Dev"
  short_description: "Run worker, critic, and remediation waves"
  default_prompt: "Use $app-dev to execute one repo-scoped ledger queue through bounded worker changes, immutable critic review, remediation tasks, and a workflow-v3 handoff."
