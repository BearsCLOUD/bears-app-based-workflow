interface:
  display_name: "App Dev"
  short_description: "Orchestrate persistent repo-L2 queues"
  default_prompt: "Use $app-dev to run repo-scoped queues, one-task app-worker dispatch in reusable repo-wave sessions, immutable review, and remediation."
