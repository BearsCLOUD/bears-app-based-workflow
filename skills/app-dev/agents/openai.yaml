interface:
  display_name: "App Dev"
  short_description: "Execute and review tasks sequentially"
  default_prompt: "Use $app-dev to implement, digest, review, and correct one workflow task at a time in sequence."
