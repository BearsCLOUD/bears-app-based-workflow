interface:
  display_name: "App Functional Graph"
  short_description: "Define graph and repo task contracts"
  default_prompt: "Use $app-functional-graph to map wave requirements, maintain graph anchors, and define the canonical repo-scoped task contract."
