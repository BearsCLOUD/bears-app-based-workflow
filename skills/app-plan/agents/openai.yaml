interface:
  display_name: "App Plan"
  short_description: "Replace the sequential task plan"
  default_prompt: "Use $app-plan to query the workflow graph and atomically replace one dependency-ordered sequential task plan."
