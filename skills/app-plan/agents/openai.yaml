interface:
  display_name: "App Plan"
  short_description: "Plan approved sequential microtasks"
  default_prompt: "Use $app-plan to turn constitution-backed research into approved ordered ledger microtasks before graph modeling."
