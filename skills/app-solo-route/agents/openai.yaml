interface:
  display_name: "App Solo Route"
  short_description: "Route all DIRECT app stages"
  default_prompt: "Use $app-solo-route to keep one DIRECT primary across workflow v3 until audited, waiting, blocked, or a user decision is required."
