interface:
  display_name: "App Specify"
  short_description: "Produce decision-complete app behavior"
  default_prompt: "Use $app-specify to resolve product decisions with the user and emit stable specification, decision, requirement, and dimension-hint refs."
