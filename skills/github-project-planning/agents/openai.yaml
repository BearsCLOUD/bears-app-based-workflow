interface:
  display_name: "GitHub Project Planning"
  short_description: "Plan non-app GitHub Projects"
  default_prompt: "Use $github-project-planning only for non-app GitHub Projects with fields, views, Issues, metadata gates, and consumer workflow inputs; route app workflow planning to $app-plan."
