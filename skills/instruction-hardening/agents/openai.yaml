interface:
  display_name: "Instruction Hardening"
  short_description: "Harden agent instructions and prompts"
  default_prompt: "Use $instruction-hardening to rewrite these agent instructions into stricter, lower-drift policy without weakening required controls."
policy:
  allow_implicit_invocation: true
