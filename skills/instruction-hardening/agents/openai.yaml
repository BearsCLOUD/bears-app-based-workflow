interface:
  display_name: "Instruction Hardening"
  short_description: "Apply the policy editing method"
  default_prompt: "Use $instruction-hardening as a method to normalize approved meaning without taking ownership from the invoking role."
