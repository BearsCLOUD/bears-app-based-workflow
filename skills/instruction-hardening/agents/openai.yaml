interface:
  display_name: "Instruction Hardening"
  short_description: "Compress approved instructions"
  default_prompt: "Use $instruction-hardening to route approved semantics through the dedicated instruction editor."
