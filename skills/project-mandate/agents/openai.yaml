interface:
  display_name: "Bears Project Mandate"
  short_description: "Bounded project packet for one registered Bears target"
  default_prompt: "Run project_registry_gate.py mandate-packet for the exact target. Read only packet read_paths and the exact target if needed. Do not scan /srv/bears or build an artifact inventory."
