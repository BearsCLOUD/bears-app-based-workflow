interface:
  display_name: "Python Codeflow"
  short_description: "Hard Python dev/review/refactor rules."
  default_prompt: "Use $python-codeflow for Python development, review, or refactoring in this repository."
policy:
  allow_implicit_invocation: true
