openai:
  display_name: "Subagents Roles"
  short_description: "Map sequential roles"
  default_prompt: "Use $subagents-roles to map graph-backed tasks to owner, critic, and helper roles for sequential handoff."
