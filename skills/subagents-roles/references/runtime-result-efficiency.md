# Runtime result efficiency

A change that affects an executable consumer records the consumer, the expected result-time or resource effect, and the evidence source.

Evidence sources include GitHub Checks, GitHub Actions logs or artifacts, Kubernetes workload status, runtime health output, Dagger ObjectiveRuntimeProof packets, or local `@Bears` CD status.
