interface:
  display_name: "Subagents"
  short_description: "Dispatch one bounded L3 result"
  default_prompt: "Use $subagents to dispatch one bounded worker, reviewer, or analyst while the wave owner retains every workflow write."
