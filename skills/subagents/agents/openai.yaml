interface:
  display_name: "Subagents"
  short_description: "Dispatch one bounded L3 assignment"
  default_prompt: "Use $subagents to dispatch one bounded L3 assignment, reuse app-worker only for one current canonical task at a time, and start a fresh immutable reviewer."
