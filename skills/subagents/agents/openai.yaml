interface:
  display_name: "Subagents"
  short_description: "Dispatch one delegation-v2 L3 assignment"
  default_prompt: "Use $subagents to select one installed L3 profile, dispatch one bounded packet v3, preserve its session identity, and keep journal ownership with the caller."
