interface:
  display_name: "Subagents"
  short_description: "Choose and dispatch one bounded L3 role"
  default_prompt: "Use $subagents to deterministically choose one installed profile and dispatch one bounded L3 assignment."
