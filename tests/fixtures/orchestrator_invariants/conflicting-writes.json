{
  "schema": "bears-orchestrator-model.v1",
  "model_id": "fixture-conflicting-writes",
  "goal_id": "fixture-conflicting-writes",
  "action": "execution",
  "scope": "safe",
  "manual_review": false,
  "evidence": ["authority", "prompt_packet", "decision_proof", "fresh_context", "durable_json_source"],
  "write_sets": [
    {"worker": "a", "paths": ["scripts/shared.py"]},
    {"worker": "b", "paths": ["scripts/shared.py"]}
  ],
  "blockers": [],
  "evidence_paths": ["tests/fixtures/orchestrator_invariants/conflicting-writes.json"]
}
