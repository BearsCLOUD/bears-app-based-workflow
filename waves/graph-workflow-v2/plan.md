# Graph Workflow v2 Plan

## Tasks

1. `TASK-CONTRACT-001`: add workflow, handoff, functional-map, traceability, process, and context-result contracts.
2. `TASK-INDEX-001`: add `app-context-index` and separate semantic-map ownership from derived-index ownership.
3. `TASK-CYCLE-001`: move all app stages to the v2 handoff and single workflow definition.
4. `TASK-MCP-001`: add the bounded read-only graph engine and bundled stdio MCP server.
5. `TASK-PACKAGE-001`: include MCP metadata, runtime portability roots, README, changelog, and cachebuster updates.
6. `TASK-AUDIT-001`: materialize this plugin's trace/process indexes and request immutable autoCI assessment for the task-scoped commit.

Tasks 1–5 are implemented in the task-scoped change. Task 6 remains `ready` until exact automation evidence exists; agent-authored artifacts do not convert it to `pass`.
